        <!-- NavBar -->
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('backoffice_home')  }}">
            <img src="{{ asset('images/LogoridersV1.1.png') }}" alt="" width="100" height="80" class="d-inline-block align-text">
            High Riders | {% if not app.user %}Connectez vous pour acceder à votre Back-Office{% endif %}
            </a>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
            {% if app.user %}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link{{ currentRoute == 'backoffice_home' ? ' active':'' }}" aria-current="page" href="{{ path('backoffice_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{ currentRoute == 'backoffice_spots' ? ' active':'' }}" href="{{ path('backoffice_spots') }}">Spots</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{ currentRoute == 'backoffice_events' ? ' active':'' }}" href="{{ path('backoffice_events') }}">Evenements</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link{{ currentRoute == 'backoffice_user_index' ? ' active':'' }}" href="{{ path('backoffice_user_index') }}">Utilisateurs</a>
                    </li>
                {# {% endif %} #}

                    {% if is_granted('ROLE_SUPER_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link{{ currentRoute == 'backoffice_contact' ? ' active':'' }}" href="{{ path('backoffice_contact') }}">Messages</a>
                        </li>
                    {% endif %}

                {# {% if app.user %} #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.user.pseudo}}
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <li>
                        <a class="dropdown-item" href="{{ path('backoffice_profil_index', {'id': app.user.id}) }}">Profil</a>
                    </li>
                    <li>
                        <hr class="dropdown-divider">
                    </li>
                    <li>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                        </ul>
                    </li>
                </ul>
                {% endif %}
                
            </div>
        </div> 

    </nav>
</header>