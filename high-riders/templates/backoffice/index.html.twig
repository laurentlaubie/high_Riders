{% extends 'backoffice/layout.html.twig' %}

{% block title %}Page d'administration
{% endblock %}

{% block content %}
        <header>

        {# <div class="container">
           
            <div class="btn-group m-4" role="group" aria-label="Basic example">
             {% if app.user %}
                <div class="badge bg-primary text-center" style="width: 10rem;">
                    {{ app.user.firstname }}
                 </div>

                <a type="button" class="btn btn-primary" href="{{ path('backoffice_user_show', {id:app.user.id}) }}">Profil</a>
                 {% if is_granted('ROLE_ADMIN') %}
                 <a type="button" class="btn btn-primary" href="{{ path('backoffice_home') }}">Back Office</a>
                 {% endif %}
                 <a type="button" class="btn btn-primary" href="{{ path('app_logout') }}">Déconnexion</a>
             {% else %}    
              <a type="button" class="btn btn-primary" href="{{ path('app_login') }}">Se connecter</a>
            </div>
             {% endif %}
        </div>
    </header> #}
  
    <main class="container">

        <h2>Page d'administration</h2>
        <p>
            Bienvenue sur High Riders
        </p>
        <div class="m-4">
            <img src="https://image.freepik.com/photos-gratuite/beau-paysage-montagnes-rocheuses-paysage-verdoyant-sous-ciel-nuageux_181624-26086.jpg" alt="Backgound High Rider">
        </div>
    </main>

    </main>
{% endblock %}
