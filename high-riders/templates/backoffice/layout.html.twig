{% extends "base.html.twig" %}

{% block body %}

        <!-- NavBar -->

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('backoffice_home')  }}">
            <img src="{{ asset('images/LogoridersV1.1.png') }}" alt="" width="100" height="80" class="d-inline-block align-text">
            High Riders |
            </a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('backoffice_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('backoffice_spots') }}">Spots</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('backoffice_events') }}">Evenements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('backoffice_user_index') }}">Utilisateurs</a>
                </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                     <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    {{ app.user.pseudo}}
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                 <li>
                    <a class="dropdown-item" href="#">Profil</a>
                </li>
                <li>
                    <hr class="dropdown-divider">
                </li>
                <li>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
                    </ul>
                </li>
            {% endif %}
            </ul>
        </div>
    </nav>


            <!-- Main content -->

            <div class="col-8">

                {% block content %}{% endblock %}

            </div>
        </main>

{% endblock %}
